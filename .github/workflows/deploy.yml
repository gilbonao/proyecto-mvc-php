name: 'GitHub Action AGilbon'

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Mostrar mensaje'
        run: echo "Hola a todos"
